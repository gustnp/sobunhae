# 소분해 — 작업 인수인계 (HANDOFF)

이 파일은 다른 기기/새 세션에서 이어서 작업할 Claude를 위한 문서예요.
**새 세션에서 이 repo를 열면 가장 먼저 이 파일을 읽고 시작하세요.** 사용자(박현수, hyunsoo.park@navercorp.com)에게
그동안 있었던 일을 다시 설명하게 하지 않아도 되도록, 지금까지의 결정·이유·남은 일을 여기 정리해둡니다.

- 서비스: 소분해 (Costco 대용량 상품 소분/공동구매 매칭 앱, 분당동 기준 하이퍼로컬)
- 저장소: gustnp/sobunhae (GitHub)
- 라이브: https://gustnp.github.io/sobunhae/ (index.html), https://gustnp.github.io/sobunhae/admin.html (관리자, water8495@gmail.com 구글 계정으로만 로그인 가능)
- 스택: 정적 HTML/JS (빌드 없음) + Firebase (Firestore, Storage, Auth, Analytics). GitHub Pages로 배포 — main에 push하면 곧바로 라이브에 반영됨 (스테이징 없음).

## ⚠️ 가장 먼저 확인할 것: 여러 세션이 동시에 작업 중일 수 있음

사용자가 claude.ai 채팅과 이 Cowork 세션을 **병행해서** 쓰고 있고, 서로의 변경사항을 모른 채 각자
index.html/admin.html을 건드리는 일이 실제로 있었음 (2026-07-28, 서로 다른 두 스레드가 완전히 다른
기반 버전 위에서 각각 작업하다가 나중에 수동으로 병합함). **새 세션을 시작할 때 "지금 라이브에 뭐가
실제로 올라가 있는지"를 절대 가정하지 말고, 사용자에게 최신 index.html/admin.html을 직접 받아서
확인할 것.** 이 문서에 적힌 "완료" 표시도 실제로 GitHub에 push됐다는 보장은 아님 (아래 "배포 상태" 참고).

## 배포 상태 (2026-07-28 기준, 이 세션 종료 시점)

**아직 GitHub에 반영 확인 안 됨.** 이 세션(Cowork)은 push 권한이 없어서 파일을 사용자에게 건네주기만
했고, 실제로 GitHub 웹 UI로 업로드했는지는 확인 안 됨. 다음 세션은 시작하자마자 실제 라이브 페이지
소스를 보고 아래 기능들이 반영됐는지부터 확인할 것.

전달한 최종 파일:
- `index.html` (최신 버전, 이 세션에서 index_v9.html로 부르던 것) — 아래 "포함된 기능" 전부 반영됨.
- `admin.html` (admin_v7.html) — 위와 동일.
- `firestore_rules_full.txt` — Firebase 콘솔 Firestore 규칙 전체 교체본. **아직 콘솔에 배포 확인 안 됨.**
- Storage 규칙: `receipts/` 경로 쓰기 규칙이 필요한데, 기존 Storage 규칙 원문을 못 받아서 **아직 작성도
  못 함.** 다음에 이어받으면 사용자에게 현재 Storage 규칙(콘솔 > Storage > Rules) 전체를 요청해서
  `deals/` 경로와 동일한 조건으로 `receipts/` 경로 규칙을 추가해줄 것.

## index.html / admin.html에 들어있는 기능 (2026-07-28 기준 최신 상태)

**신뢰/보안**
- 닉네임 기반 식별 + 익명 인증(Anonymous Auth) uid 매칭으로 "같은 기기가 그 닉네임으로 실제 호스트를
  했었는지"까지 확인 (완벽한 신원 확인은 아님, hostTrust()/myHostTrust() 참고).
- 게시판 글쓰기는 좋은 후기 3개 이상(나쁜 후기 0) 닉네임만 가능. **운영자(water8495@gmail.com)는
  index.html 풋터의 "운영자 로그인" 링크로 구글 로그인하면 이 조건을 건너뜀** — 예전엔 "안정환" 같은
  닉네임 문자열을 그대로 매칭하는 방식이었는데, 이건 아무나 그 문자열을 타이핑하면 뚫리는 가짜 인증이라
  구글 로그인(isOperator()) 기반으로 교체함.
- 닉네임 차단(bannedNicknames 컬렉션) — admin.html에서 차단/차단해제 가능, 게시판 글/소분 참여 시
  차단 여부 체크.
- Firestore 보안 규칙 전체를 다시 점검함 — 배포돼 있던 규칙에 `/config/{docId}` 앞 `match` 키워드가
  빠져있었고(문법 오류), `bannedNicknames` 규칙이 아예 없었고(차단 기능이 조용히 실패했을 가능성),
  `posts`/`comments`/`deals/messages`에 로그인 요구 조건이 없어서 devtools로 API를 직접 찌르면
  아무나 글을 쓸 수 있는 상태였음. `firestore_rules_full.txt`에 전부 반영해서 새로 작성함 (아래
  "다음에 할 일" 참고 — 아직 콘솔 배포 확인 안 됨).

**핵심 기능**
- 소분(공동구매) 등록/참여/정산 — 상태는 모집중(open)/모집완료(matched)/거래완료(completed) 3단계.
  카드 배지·진행바 색으로 구분 (이모지 안 씀 — 아래 "디자인 원칙" 참고).
- 지도 보기 (카카오맵) — 분당동 기준 고정 랜드마크 마커 포함, 클릭하면 정보창 뜸.
- 배너 캐러셀 (최대 4개) + 점(dot) 인디케이터로 몇 번째 배너인지 표시, 점 클릭으로 수동 이동 가능.
- 키워드 추천 배너, 이번주 인기 소분템 랭킹, 관심 키워드(⭐, localStorage).
- 소분 카드 공유하기 (Web Share API, `?deal=id` 딥링크로 특정 카드까지 스크롤+하이라이트).
- **게시판**: 일반/사다드림/공지(notice) 3종. 공지는 admin.html에서만 작성 가능하고 게시판 최상단에
  항상 고정 정렬됨 (일반 유저는 못 씀). 댓글은 펼칠 때 1회성 조회(실시간 구독 아님 — 비용 절감).
- **참여자 채팅** (신규): "인원 마감" 상태 카드에 "참여자 채팅 열기" 버튼 → `deals/{id}/messages`
  서브컬렉션 기반 실시간 채팅 모달. 기존 카카오톡 오픈채팅 링크는 부가 옵션으로 그대로 유지.
- **영수증 첨부** (신규): 모집완료/거래완료 카드에 영수증 사진 업로드(`uploadImageFile()` 공용
  헬퍼로 Storage 업로드) + "영수증 보기" 링크. 정산 금액 분란 방지 목적. `deals.receiptUrl` 필드 사용.
- 노쇼 신고 → admin.html에서 닉네임 차단까지 연결.

**모바일 대응**
- viewport-fit=cover + `env(safe-area-inset-bottom)` — 하단 고정 CTA 버튼이 아이폰 사파리 하단 바에
  붙어 답답해 보이던 문제 수정.
- `html, body { overflow-x: hidden; max-width:100% }` 안전장치 — 필터 줄(전체/모집중/모집완료/거래완료
  + 동이름 + 태그) 폭이 좁은 화면보다 넓어서 화면 전체가 옆으로 밀리던(빈 공간 생기던) 버그가 있었음.
  근본 원인이던 "거래완료" 필터 칩은 제거함(상태 자체는 카드 배지로 계속 보임, 필터 용도만 뺌).

**디자인 원칙 (사용자가 명시적으로 요청한 것 — 다음에도 지킬 것)**
- 배지/상태 표시에 이모지 쓰지 말 것. `.ad-sticker` 클래스 + 색상 텍스트로만 구분 (예: "거래 완료",
  "인원 마감", "공지", "사다드림", "일반" — 전부 이모지 없이 텍스트+배경색만).
- 게시판에 타입 필터 탭(전체/일반/사다드림) UI는 없앤 상태 유지 — 사용자가 필요없다고 명시함. 카드
  배지로만 구분.
- 사용자는 모바일 최적화를 항상 기본값으로 요구함. 새 UI는 모바일 폭 기준으로 먼저 생각할 것.
- 소통은 간결하게 — 불필요한 요약/서론 없이 결과 위주로.

## 이번에 발견했지만 일부러 안 건드린 것 (다음에 필요하면 참고)

- admin.html에 "노쇼 누적 자동 감지 → 차단 후보 제안" 리스트(`noShowReports` 컬렉션 집계 기반)를
  추가한 다른 버전이 있었음. 지금 admin.html은 수동 입력식 차단 관리만 있고 이 자동 제안 기능은
  아직 병합 안 함 — 사용자가 원하면 다음에 추가.
- posts 삭제 시 comments 서브컬렉션도 함께 정리하도록 admin.html 수정함 (예전엔 게시글만 지우고
  댓글은 orphan으로 남았음).

## 남은 보안 과제 (의도적으로 미룬 것)

- participants가 배열이라 Firestore 규칙에서 "배열 원소가 진짜 요청자 본인 것"까지는 검증 못 함 —
  민감 필드(계좌/정산방식/가격/인원) 잠금으로 대신 방어 중. 제대로 하려면 participants를
  서브컬렉션(문서마다 uid 소유)으로 구조 변경 필요 (큰 리팩터, 트래픽 늘면 우선순위 올릴 것).
- Rate limiting / anti-abuse 없음 (Firestore 규칙 기본 검증 수준). App Check 등 고려 가능.

## 다음에 할 일 (우선순위)

1. **Storage 규칙에 `receipts/` 경로 추가** — 현재 Storage 규칙 원문을 사용자에게 요청해서
   `deals/`와 동일 조건으로 추가해야 영수증 업로드가 실제로 동작함.
2. **firestore_rules_full.txt 콘솔 배포 확인.**
3. **index.html/admin.html 실제 GitHub 업로드 확인** — 여러 세션이 각자 파일을 건네준 상태라 혼선
   있었음, 최종본이 진짜 반영됐는지부터 확인.
4. 공지(notice) 실제 내용 등록 — 소분 원칙(수고비 없음/영수증 기준 정산), 사다드림 규칙(댓글 신청·
   채팅 조율), 노쇼 페널티(1회 경고 → 2회 제한 → 3회 강퇴) 안내문을 admin.html에서 직접 작성해야 함
   (아직 빈 상태).

## 수익화 마일스톤 (2026-07-27 정리, 실제 사업성 논의 기준)

- **실제 수익 채널: 쿠팡 파트너스 배너 링크** (배너 클릭 → 쿠팡 구매 시 수수료). 광고 네트워크(구글
  애즈/카카오 애드핏)는 채택 안 함 — 트래픽 규모상 무의미한 수준으로 결론남.
- 마일스톤 0(완료): 디자인/기능 정비, 신뢰·차단 체계, 관리자 로그인 복구, 지도 마커, 공유하기,
  영수증/공지/채팅, 모바일 버그 수정.
- 마일스톤 1(지금, 사용자 담당, 가장 시급): 분당동 실제 이웃 모으기. 콜드스타트라 코드로 해결 불가.
- 마일스톤 2(지금 가능): 쿠팡 파트너스 가입 → 배너 슬롯에 링크 등록.
- 마일스톤 3(이웃 어느 정도 모인 뒤): PWA 설치 유도 + 웹 푸시(Cloud Function, Blaze 요금제 필요),
  participants 서브컬렉션화.
- 마일스톤 4(트래픽 실제로 늘었을 때): 서버사이드 검색, 정산 신뢰 강화(에스크로 등), 광고 매체 재검토.
- 냉정한 평가 결론: 하이퍼로컬 특성상 신뢰 유지(작은 규모)와 광고 수익(큰 트래픽 필요)이 구조적으로
  충돌함. 지역 업체 협찬이 그나마 스케일 없이 되는 유일한 수익 경로지만 용돈 수준. 이 프로젝트의
  진짜 가치는 "사업"보다 "동네 도움 + 포트폴리오"로 보는 게 현실적.

## 작업 방식 참고사항 (반복 실수 방지용)

- **배포 확인**: 로컬 파일과 실제 라이브 페이지를 `cache: no-store`로 fetch해서 문자 수 비교. 배포는
  40초~5분 걸릴 수 있어 재시도 필요.
- **Firestore 직접 쓰기 테스트 시 주의**: 진짜 프로덕션 DB. "실패"처럼 보여도 조용히 실행됐을 수 있어
  재조회로 반드시 재확인.
- **Storage 규칙**: 2026년 2월부터 Storage가 Spark(무료) 플랜에서 빠지고 Blaze(결제수단 등록) 필수로
  바뀜 — 실제 청구액은 거의 0원이겠지만 예산 알림 걸어두길 권장.
- **git push 권한 없음**: 이 세션 종류(Cowork/claude.ai)는 GitHub push를 못 함 — 파일을 완성해서
  사용자에게 건네주면 사용자가 GitHub 웹 UI로 직접 업로드하는 방식으로 진행 중.
